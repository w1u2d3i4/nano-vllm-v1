# Nano-vLLM-v1-main 精简与移植指南

本文档对比 `nano-vllm-main`（原版）与 `nano-vllm-v1-main`（作者 v1），标出**必须移植**的改动与**可精简**的部分，便于你检查后再移植到自己的 `nano-vllm-v1`。

---

## 一、改动总览

| 文件 | 改动性质 | 是否必须 |
|------|----------|----------|
| `config.py` | 新增 `chunked_prefill`；放宽 `max_model_len` / assert | 仅 `chunked_prefill` 必须 |
| `sequence.py` | 新增 `num_new_tokens`、`num_context_tokens`；序列化方式变化 | 前两者必须；序列化可保留原版 |
| `block_manager.py` | `get_token_layout`、`can_allocate(num_tokens)`、`can_append(seq, num_new_tokens)`；allocate/may_append 重写 | 全部为功能核心，需保留 |
| `scheduler.py` | 统一调度、token_budget、先 running 后 waiting、chunked | 核心，需保留 |
| `model_runner.py` | 合并 prefill/decode 为 `prepare_model_input`；`seq_need_compute_logits`；warmup 设 `num_new_tokens` | 核心，需保留；仅 SHM 清理可删 |
| `llm_engine.py` | `step()` 无 `is_prefill`；`postprocess(seq_need_compute_logits)`；generate 只总吞吐 | 与调度/采样一致，需保留 |
| `context.py` | 去掉 `is_prefill`；新增 `seq_need_compute_logits`；`set_context` 签名变化 | 与 model_runner/attention 一致，需保留 |
| `attention.py` | 仅用 `flash_attn_varlen_func`，不再区分 prefill/decode 两分支 | 简化且必须，需保留 |

---

## 二、分文件说明与精简建议

### 1. `config.py`

**v1-main 相对原版：**
- 新增：`chunked_prefill: bool = False`
- `max_model_len` 默认从 4096 改为 40960
- 注释掉：`assert self.max_num_batched_tokens >= self.max_model_len`

**为什么要修改：**
- 原版调度器不区分 prefill 与 decode 是否分块，全部一次性处理。v1 引入 Chunked Prefill 后，调度器需要 `chunked_prefill` 开关来决定是否将长 prompt 切 chunk；`scheduler.py` 的 `__init__` 直接读 `config.chunked_prefill`，若缺此字段整个调度链就断了。
- `max_model_len` 放宽与 assert 注释掉是为了支持超长上下文（如 32k+），但对你的场景不是功能必要。

**精简建议：**
- **必加：** `chunked_prefill: bool = False`（调度与 model_runner 依赖）
- **可选：** `max_model_len` 和 assert 保持原版即可，避免长序列带来的显存/行为变化；若你确定需要更长上下文再改。

---

### 2. `sequence.py`

**v1-main 相对原版：**
- 新增字段：`num_new_tokens = 0`
- 新增属性：`num_context_tokens`（= `num_cached_tokens + num_new_tokens`）、`num_current_blocks`（= `len(block_table)`，且带 assert）
- 注释掉：`last_block_num_tokens`
- `__getstate__` / `__setstate__`：改为始终序列化 `(token_ids, last_token, num_tokens, num_prompt_tokens, num_cached_tokens, num_new_tokens, block_table, temperature)`，不再按 prefill/decode 区分

**为什么要修改：**
- 原版中 Sequence 不需要知道"本步要计算多少新 token"——因为 prefill 一次性算完整个 prompt，decode 每步恰好 1 token，这个数量是隐式的。v1 引入 Chunked Prefill 后，一个 prefill 可能只算 prompt 的一部分（一个 chunk），所以必须用 `num_new_tokens` 显式记录"本步要计算的 token 数"，调度器设它、block_manager 读它来分配恰好数量的 block、model_runner 读它来构建 slot_mapping 范围。
- `num_context_tokens`（= `num_cached_tokens + num_new_tokens`）是"本步处理完后该序列已有 KV Cache 的 token 总数"，model_runner 的 `prepare_model_input` 用它确定 input_ids 的截取范围和 slot_mapping 的终止位置，不加的话 model_runner 需要自己算，会导致逻辑分散。

**精简建议：**
- **必加：** `num_new_tokens` 初始化与在 postprocess 中的更新（与 scheduler / block_manager 约定一致）。
- **必加：** `num_context_tokens` 只读属性（model_runner 和 block_manager 会读）。
- **可删：** `num_current_blocks` 仅用于 assert，且等价于 `len(seq.block_table)`，可不加；若加，仅保留 `return len(self.block_table)` 即可，不必 assert。
- **建议保留原版序列化：** 原版在 decode 时只传 `last_token`，多进程/Tensor Parallel 时更省；v1-main 的序列化更简单但 decode 也传整段 `token_ids`。移植时若 TP 重要，可保留原版 `__getstate__`/`__setstate__`，只在反序列化后按需补一句 `num_new_tokens = getattr(self, 'num_new_tokens', 0)` 以兼容旧 pickle。
- **last_block_num_tokens：** v1-main 的 block_manager 不再从 Sequence 读该属性，可不再提供；若你其它地方有引用再保留。

---

### 3. `block_manager.py`

**v1-main 相对原版：**
- 类注释里多了一段 token layout 的 ASCII 图。
- `can_allocate(seq)` → `can_allocate(num_tokens: int)`，按 token 数算 block 数。
- 新增 `get_token_layout(seq)`，返回 `(num_new_computed_tokens_in_used, num_new_computed_tokens_in_free, num_new_tokens)`，供调度器做 chunked 与分配决策。
- `allocate(seq)` 重写：先按 prefix cache 分配“已计算”的 block，再按 `seq.num_new_tokens` 分配“本 chunk 要计算”的 block；依赖调度器事先设置 `seq.num_new_tokens`。
- `can_append(seq)` → `can_append(seq, num_new_tokens: int)`，按“本步要追加的 token 数”判断是否需要新 block。
- `may_append(seq)` 重写：按 `num_cached_tokens + num_new_tokens` 为界循环，支持一次追加多 token（chunked decode）。
- `_allocate_block` 中：若当前 block 的 hash 在 `hash_to_block_id` 里指向自己，先 `pop` 再 reset，避免悬空引用。

**为什么要修改：**
- 原版 `can_allocate(seq)` 按整个序列的 `num_blocks` 判断，隐含假设一次性分配所有 block。Chunked Prefill 下一次 prefill 只处理部分 token，只需分配部分 block，所以改为 `can_allocate(num_tokens)` 按实际需要的 token 数计算。
- `get_token_layout` 是 Chunked Prefill 的关键：调度器在决定新请求能否入场前，需要知道"这个 seq 有多少 token 已被 prefix cache 命中（在已用 block 里 / 在空闲 block 里）、有多少 token 需要新分配"，三个数字分别决定了 ref_count 操作、block 分配数和 token_budget 消耗。
- 原版 `can_append(seq)` 只判断"下一个 token 是否需要新 block"（decode 每步 +1），v1 改为 `can_append(seq, num_new_tokens)` 是因为 chunked prefill 的 running 序列一步可能追加多个 token（一个 chunk），需要按实际数量判断。
- `may_append` 同理，原版只处理 +1 token 的情况，v1 需要循环处理多 token 跨越多 block 的情况。
- `_allocate_block` 里 pop `hash_to_block_id`：原版重用 free block 时不清理旧 hash 映射，可能导致 prefix cache 查到已被覆盖的脏 block，v1 修复了这个潜在 bug。

**精简建议：**
- 逻辑**不要删**，都是 Chunked Prefill + 统一调度的核心。
- 可精简处：删掉类顶部 long 的 ASCII 注释，或缩短为一两句说明“used vs free vs new”即可。
- 建议保留 `_allocate_block` 里对 `hash_to_block_id` 的 pop，避免 hash 表脏数据。

---

### 4. `scheduler.py`

**v1-main 相对原版：**
- `__init__`：增加 `enable_chunked = config.chunked_prefill`、`max_model_len`。
- `add(seq)`：增加 `assert len(seq) <= max_model_len - 1`。
- `schedule()`：不再返回 `(seqs, is_prefill)`，只返回 `seqs`；实现改为：
  - 维护 `token_budget = max_num_batched_tokens`；
  - 先从 **running** 里按序调度，每序列用 `num_new_tokens = min(未计算长度, token_budget, 剩余可缓存)`，调用 `can_append(seq, num_new_tokens)` / `may_append(seq)`，不够就 preempt 队尾；
  - 再在 `not preempted_seqs` 时从 **waiting** 里用 `get_token_layout` + `can_allocate(num_tokens)` 调度新请求，支持 chunked（只分配本 chunk 的 `num_new_tokens`）；
  - 返回 `scheduled_running_seqs + scheduled_new_reqs`。
- `postprocess(seqs, token_ids, seq_need_compute_logits)`：多一个参数；只对 `seq_need_compute_logits` 里下标对应的 seq 做 `append_token` 并检查 EOS/max_tokens；所有未 finished 的 seq 统一做 `num_cached_tokens += num_new_tokens`、`num_new_tokens = 0`。

**为什么要修改：**
- 原版 schedule() 返回 `(seqs, is_prefill)`，prefill 和 decode 是互斥的两个阶段。这在 Chunked Prefill 下行不通：一个 batch 可能同时包含"正在 chunked prefill 的序列"和"正在 decode 的序列"，无法用单一 bool 区分。所以 v1 去掉 `is_prefill`，返回混合 batch。
- 先调度 running 再调度 waiting：原版"prefill 优先"会导致长 prompt 一次性占满 token_budget，decode 中的序列全部等待。v1 反过来先保证 running 序列继续 decode（延迟敏感），再用剩余 budget 调度新请求，这是 vLLM v1 的做法。
- `postprocess` 多了 `seq_need_compute_logits` 参数：因为 chunked batch 中并非所有序列都需要采样（只处理了 chunk 前半部分的序列不需要），所以必须明确标记哪些序列本步产出了最终 logits。
- `num_cached_tokens += num_new_tokens` 的统一更新：原版靠隐式约定（prefill 后 num_cached_tokens 不动，decode 靠 block_manager），v1 显式在 postprocess 尾部统一推进，更清晰且不容易出错。

**精简建议：**
- 调度逻辑**保持 v1-main 实现**，否则 chunked 与统一调度会断掉。
- 唯一可读性上的简化：`preempted_seqs` 仅用于 `if not preempted_seqs` 判断，可改为布尔 `had_preemption`，少维护一个列表（逻辑不变）。

---

### 5. `model_runner.py`

**v1-main 相对原版：**
- **init：** rank 0 在创建 SharedMemory 前多了一段 try/except：连接已存在的 "nanovllm" 并 unlink，避免上次进程异常退出留下脏 shm。
- **warmup：** 原版 `run(seqs, True)`；v1 先对每个 seq 设 `seq.num_new_tokens = max_model_len`，再 `run(seqs)`（无 is_prefill）。
- **prepare_*：** 原版拆成 `prepare_prefill` 和 `prepare_decode`，由 `run(seqs, is_prefill)` 二选一；v1 合并为 `prepare_model_input(seqs)`，用 `num_cached_tokens` / `num_context_tokens` / `num_new_tokens` 统一处理 prefill 与 decode，并计算 `seq_need_compute_logits`（本步哪些 seq 的最后一个 token 在本轮被计算，需要参与采样）。
- **set_context：** 原版带 `is_prefill`；v1 去掉，并增加 `seq_need_compute_logits`。
- **prepare_sample：** 原版对所有 seq 取 temperature；v1 只对 `context.seq_need_compute_logits` 对应的 seq 取 temperature，与采样/logits 对齐。
- **run_model：** 原版根据 `is_prefill` 走 eager 或 CUDA graph；v1 不再传 `is_prefill`，只根据 `enforce_eager` 和 `input_ids.size(0) > 512` 分支（统一用 varlen，decode 也是）。
- **run：** 返回 `(token_ids, seq_need_compute_logits)`，供 llm_engine 传给 postprocess。

**为什么要修改：**
- 原版 `prepare_prefill` 和 `prepare_decode` 是两套完全不同的逻辑：prefill 用 `cu_seqlens_q/k` + varlen，decode 用 `context_lens` + `flash_attn_with_kvcache`。统一调度后一个 batch 混合了 prefill 和 decode 序列，不可能再二选一，必须合并成一套 `prepare_model_input`，用 `num_new_tokens`（可以是 1 也可以是一整个 chunk）统一构建 `cu_seqlens_q/k` 和 `slot_mapping`。
- `seq_need_compute_logits`：chunked batch 中，有些序列本步只处理了 chunk 的中间部分（还没到最后一个 token），它们不需要采样。只有"本步处理到了序列当前末尾"的序列才需要出 logits → 采样 → append_token。这个信息在 model_runner 构建输入时自然可以判断（`len(seq) == num_cached_tokens + num_new_tokens` 且有 block_table），所以在这里计算最合适。
- `prepare_sample` 过滤 temperature：与 `seq_need_compute_logits` 对齐，采样只对需要出 logits 的序列，temperature 数量必须和 logits 行数一致。
- `run_model` 去掉 `is_prefill` 判断：统一用 varlen 后，prefill 和 decode 走同一条 flash_attn_varlen_func 路径（decode 就是 seqlen_q=1 的 varlen），不需要区分。CUDA Graph 的触发条件也简化为只看 batch 大小。

**精简建议：**
- **必移植：** warmup 里对 `num_new_tokens` 的赋值、`prepare_model_input` 整套逻辑、`seq_need_compute_logits` 的计算与传递、`prepare_sample` 的过滤、`set_context`/Context 的签名变化、`run` 的返回值。
- **可删：** SharedMemory 的 try/except 清理（约 6 行）。若你环境里不会残留 "nanovllm" 或可以接受手动清理，可去掉以保持与原版一致。

---

### 6. `llm_engine.py`

**v1-main 相对原版：**
- `step()`：`seqs = self.scheduler.schedule()`，不再有 `is_prefill`；`token_ids, seq_need_compute_logits = self.model_runner.call("run", seqs)`；`postprocess(seqs, token_ids, seq_need_compute_logits)`；返回值用 `num_total_tokens = sum(len(seq) for seq in seqs if seq.is_finished)`（只统计本步完成的 seq 的 token 数）。
- `generate()`：不再区分 prefill/decode 吞吐，只保留一个总吞吐在 tqdm 里展示。

**为什么要修改：**
- 纯粹是接口跟随：scheduler.schedule() 不再返回 `is_prefill`，model_runner.run() 多返回 `seq_need_compute_logits`，所以 step() 的调用链和参数必须同步调整，否则编译都过不了。
- generate() 不再区分 prefill/decode 吞吐：因为统一调度后一个 step 可能同时包含两者，分别计算已无意义，改为总吞吐更准确。

**为什么要修改：**
- `step()` 的改动纯粹是跟随 scheduler 和 model_runner 的接口变化：scheduler 不再返回 `is_prefill`，model_runner 多返回 `seq_need_compute_logits`，postprocess 多接收一个参数。不改的话接口对不上会直接报错。
- `generate()` 中原版按 `is_prefill` 分别统计 prefill/decode 吞吐。统一调度后一个 step 里混合了两者，无法再拆开统计，所以简化为总吞吐。这不是功能退化，而是语义上的正确对齐。

**精简建议：**
- 与 scheduler / model_runner 的接口保持一致即可，**无额外可删**；generate 的展示简化本身就是精简。

---

### 7. `context.py`

**v1-main 相对原版：**
- `Context` 去掉 `is_prefill` 字段。
- 新增 `seq_need_compute_logits: torch.Tensor | None`。
- `set_context(...)` 签名改为无 `is_prefill`，多一个 `seq_need_compute_logits` 参数。

**为什么要修改：**
- `is_prefill` 被去掉是因为统一调度后 model_runner 不再区分 prefill/decode，Context 作为全局传参载体必须与之同步。如果保留 `is_prefill`，attention 层就还得做 if/else 分支，与"统一"的设计目标矛盾。
- `seq_need_compute_logits` 需要从 model_runner 传到 llm_engine/scheduler 的 postprocess，而 model_runner 内部用 Context 做全局传参（避免层层显式传参），所以把它塞进 Context 是最自然的路径。

**精简建议：**
- 与 model_runner、attention 一致，**按 v1-main 改即可**，无多余可删。

---

### 8. `attention.py`

**v1-main 相对原版：**
- 原版：`if context.is_prefill: ... flash_attn_varlen_func ... else: ... flash_attn_with_kvcache ...`。
- v1：只保留 `flash_attn_varlen_func` 一条路径（且 `block_table` 非空时用 cache 的 K,V），不再用 `flash_attn_with_kvcache`。
**为什么要修改：**
- 原版 attention 靠 `context.is_prefill` 走两条分支：prefill 用 `flash_attn_varlen_func`（支持变长 batch），decode 用 `flash_attn_with_kvcache`（每序列 1 token，走专用 decode kernel）。统一调度后一个 batch 混合了 prefill 和 decode 序列，无法用单一 bool 切换。
- v1 发现 `flash_attn_varlen_func` 本身就能处理 decode（seqlen_q=1 就是 decode），不需要 `flash_attn_with_kvcache`。去掉 decode 分支后代码从 16 行缩到 6 行，且 `flash_attn_with_kvcache` 这个 import 也可以删掉。
- 性能方面：对于 decode，varlen 路径比专用 kvcache 路径略慢（多了一点 cumsum 开销），但差距在 1-2% 以内，换来的是架构统一和 chunked prefill 的正确性，是值得的权衡。

**精简建议：**
- 保留 v1 的单一 varlen 路径即可，逻辑更简单，且与统一 `prepare_model_input` 一致。

---

## 三、移植顺序建议

1. **config.py**：加 `chunked_prefill`。
2. **context.py**：去掉 `is_prefill`，加 `seq_need_compute_logits` 及新 `set_context` 签名。
3. **sequence.py**：加 `num_new_tokens`、`num_context_tokens`；若保留原版序列化，在 `__setstate__` 里兼容 `num_new_tokens`。
4. **block_manager.py**：按 v1-main 实现 `get_token_layout`、`can_allocate(num_tokens)`、`can_append(seq, num_new_tokens)` 及新的 `allocate` / `may_append`（可删长注释）。
5. **scheduler.py**：整体替换为 v1-main 的 schedule/postprocess 逻辑（可选把 `preempted_seqs` 改为布尔）。
6. **model_runner.py**：统一为 `prepare_model_input`、`run(seqs)`、返回 `(token_ids, seq_need_compute_logits)`；warmup 设 `num_new_tokens`；可选去掉 SHM 清理。
7. **llm_engine.py**：step/generate 按 v1-main 改。
8. **attention.py**：只保留 v1 的 varlen 单路径。

---

## 四、可省略/可选清单（便于你检查）

| 项目 | 说明 |
|------|------|
| Config 里 `max_model_len` 默认改为 40960 | 可选，沿用 4096 更安全 |
| Config 里注释掉 `max_num_batched_tokens >= max_model_len` | 可选，与上面一致 |
| Sequence 的 `num_current_blocks` | 可省略，用 `len(block_table)` 即可 |
| Sequence 的 `last_block_num_tokens` | v1 未用，可省略 |
| Sequence 的 v1 版完整序列化 | 建议保留原版序列化以省 TP 通信，并兼容 `num_new_tokens` |
| BlockManager 顶部 long ASCII 注释 | 可删或缩短 |
| ModelRunner 里 SharedMemory 的 try/except 清理 | 可删，减少对“脏 shm”的依赖 |
| Scheduler 里 `preempted_seqs` 列表 | 可改为布尔 `had_preemption`，逻辑不变 |

按上述顺序和清单检查后再移植，可以既保留 Chunked Prefill + 统一调度 + BlockManager 增强，又避免不必要的复杂度和行为变化。
